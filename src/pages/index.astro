---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Founded in Feb 2026">
  <section class="hero">
    <div class="chapter-number">I</div>
    <h1>Founded in Feb 2026</h1>
    <p class="subtitle">A collection of thoughts, stories, and observations<br/>by Christian McKenna</p>
    <div class="divider"></div>
  </section>

  <section class="intro">
    <p class="drop-cap">Welcome, reader. This is a quiet corner of the internet — a place for writing that takes its time. Here you'll find essays, reflections, and the occasional idea that refused to stay in my head. Pull up a chair, settle in, and stay as long as you'd like.</p>
  </section>

  <section class="recent-writings">
    <h2>Recent Writing</h2>
    <p class="section-note">New chapters as they come.</p>

    <article class="post-preview">
      <span class="post-date">February 13, 2026</span>
      <h3><a href="/foundedinfeb26/first-post">On Beginnings</a></h3>
      <p class="post-excerpt">Every story has to start somewhere. This one starts with a blank page, a blinking cursor, and the quiet conviction that some things are worth writing down.</p>
    </article>

    <div class="divider"></div>

    <div class="more-link">
      <a href="/foundedinfeb26">Browse all writing &rarr;</a>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    text-align: center;
    padding: 2rem 0 0;
  }

  .chapter-number {
    font-family: 'Cormorant Garamond', 'Georgia', serif;
    font-size: 0.95rem;
    letter-spacing: 0.15em;
    color: var(--ink-faint);
    margin-bottom: 1.5rem;
    text-transform: uppercase;
  }

  .hero h1 {
    font-size: 3rem;
    font-weight: 400;
    letter-spacing: -0.01em;
    margin-bottom: 1rem;
  }

  .subtitle {
    font-family: 'Cormorant Garamond', 'Georgia', serif;
    font-size: 1.15rem;
    font-style: italic;
    color: var(--ink-light);
    line-height: 1.6;
    text-indent: 0;
  }

  .intro {
    margin: 1rem 0 3rem;
  }

  .recent-writings h2 {
    text-align: center;
    font-weight: 400;
    letter-spacing: 0.05em;
    font-size: 1.4rem;
    text-transform: uppercase;
  }

  .section-note {
    text-align: center;
    font-style: italic;
    color: var(--ink-faint);
    font-size: 0.95rem;
    margin-bottom: 2rem;
    text-indent: 0;
  }

  .post-preview {
    padding: 1.5rem 0;
  }

  .post-date {
    font-family: 'Cormorant Garamond', 'Georgia', serif;
    font-size: 0.85rem;
    color: var(--ink-faint);
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .post-preview h3 {
    margin-top: 0.3rem;
    margin-bottom: 0.5rem;
    font-weight: 500;
    font-size: 1.5rem;
  }

  .post-preview h3 a {
    color: var(--ink);
    text-decoration: none;
    transition: color 0.3s ease;
  }

  .post-preview h3 a:hover {
    color: var(--accent);
  }

  .post-excerpt {
    color: var(--ink-light);
    text-indent: 0;
    font-size: 1rem;
  }

  .more-link {
    text-align: center;
    margin-top: 1rem;
  }

  .more-link a {
    font-family: 'Cormorant Garamond', 'Georgia', serif;
    font-size: 1rem;
    letter-spacing: 0.03em;
    text-decoration: none;
    color: var(--accent);
    transition: color 0.3s ease;
  }

  .more-link a:hover {
    color: var(--accent-hover);
  }

  @media (max-width: 600px) {
    .hero h1 {
      font-size: 2.2rem;
    }
  }
</style>
```

